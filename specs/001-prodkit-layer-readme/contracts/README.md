# Contracts (N/A): Prod‑Kit Overlay README

This feature does not define APIs or machine-consumable interfaces.

If future work adds a CLI, schema, or validator for enforcing Prod‑Kit gates, contracts would live
here.

